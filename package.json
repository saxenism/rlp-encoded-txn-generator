{
  "name": "txn-rlp-encoder",
  "version": "1.0.0",
  "description": "A utility tool to generate random rlp encoded transactions, initially to be used as inputs for test cases for specular L@",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "saxenism",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "web3": "^1.8.2"
  }
}
